import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aman.lensclone',
  appName: 'google-lens-ui-clone',
  webDir: 'dist'
};

export default config;
